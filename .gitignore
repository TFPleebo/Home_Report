# NEVER commit: contains the GitHub personal access token
publish_config.json

# Derived output - regenerated by merge.py on every run.
# ~3MB and would change daily, bloating git history for no benefit.
home_climate_log.csv

# Local cruft
.DS_Store
**/.DS_Store
*.bak
